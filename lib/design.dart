import 'package:flutter/material.dart';

var standardPadding = const EdgeInsets.symmetric(vertical: 16, horizontal: 20);

standardTextStyle(double? fontSize) {
  return TextStyle(fontFamily: 'DejaVuSans', fontSize: fontSize);
}